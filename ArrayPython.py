"""

NOTE : Python does not have built-in support for Arrays, but Python Lists can be used instead.

list = ["value1", "value2",..........]
list[0] = "HEllo"
x = len(list)
length of array
for x in list:
    print(x)


NOTE : The length of an array is always one more than the highest array index.
add a value in array using list.append()
pop() and remove() to remove values from array


"""

array = [23,4,5,56,"d"]   

print(array[0])
